<?xml version="1.0" encoding="utf-8"?>
<configuration>
    <contextName>demo</contextName>
    <!--格式化输出：%d表示日期，%thread表示线程名，%-5level：级别从左显示5个字符宽度%msg：日志消息，%n是换行符-->
    <property name="pattern" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg %n"/>
    <property name="pattern-color" value="%yellow(%d{yyyy-MM-dd HH:mm:ss.SSS}) [%thread] %highlight(%-5level) %green(%logger{50}) - %highlight(%msg) %n"/>
    <property name="LOG_HOME" value="logs"/>
  
    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${pattern}</pattern>
        </encoder>
    </appender>

    <!-- 控制台输出-带颜色 -->
    <appender name="CONSOLE-WITH-COLOR" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <pattern>${pattern-color}</pattern>
        </encoder>
    </appender>

    <!-- syslog -->
      <appender name="SYSLOG" class="ch.qos.logback.classic.net.SyslogAppender">
        <syslogHost>172.17.211.157</syslogHost>
        <port>514</port>
        <facility>LOCAL5</facility>
       <!--  <suffixPattern>demo</suffixPattern> -->      
       <!-- <suffixPattern>[%mdc{ABC}] %d{yyyy-MM-dd HH:mm:ss.SSS}  [%thread] %-5level  %logger %msg</suffixPattern> 这里mdc 是java 代码中加的，直接写死就是syslogtag的值 -->
       <suffixPattern>DEMO %d{yyyy-MM-dd HH:mm:ss.SSS}  [%thread] %-5level  %logger %msg</suffixPattern>
      </appender>

      <root level="DEBUG">
        <appender-ref ref="CONSOLE-WITH-COLOR"/>
        <appender-ref ref="SYSLOG" />
      </root>

</configuration>
